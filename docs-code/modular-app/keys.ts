/**
 * This file contains the binding keys for the modular application example.
 */

// Authentication binding keys
export enum AuthBindings {
  COMPONENT = 'components.Authentication',
  SERVICE = 'services.AuthService',
  PROVIDER = 'providers.AuthProvider',
}

// API binding keys
export enum ApiBindings {
  COMPONENT = 'components.Api',
  CONTROLLER = 'controllers.UserController',
}
